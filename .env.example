# Copy to .env for local development.
#
# These two values are PUBLIC. They ship in the client bundle, and the repo is
# public, so anyone can read them. That is safe only because row-level security
# does the real enforcement — see docs/DECISIONS.md#0003.
#
# Never put a Square token, a Google client secret, a Resend key, or the
# Supabase service_role key in this file or any VITE_ variable. Those live in
# Supabase edge function secrets.

VITE_SUPABASE_URL=https://bdbarnvjktpfdogcfjvx.supabase.co
VITE_SUPABASE_ANON_KEY=
